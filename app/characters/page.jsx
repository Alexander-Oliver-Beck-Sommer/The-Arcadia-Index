import React from "react";
import meda from "@/data/header.json";

export const metadata = {
  title: meda.pages.characters.title,
  description: meda.pages.characters.title,
};

const Characters = () => {
  return <div>Characters</div>;
};

export default Characters;
